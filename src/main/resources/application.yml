spring:
  application:
    name : book_db
  datasource:
    url: jdbc:postgresql://localhost:5432/book_db
    username: postgres
    password: hasan82.13
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081

#jwt:
#  secret: "mySuperSecretKeyForJWT2024!MakeItLongAndSecure12345"